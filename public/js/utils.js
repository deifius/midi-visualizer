// utils.js

export function formatNoteName(noteNumber) {
  // Simple placeholder
  return `Note ${noteNumber}`;
}

export function getChannelName(channelNumber) {
  return `Channel ${channelNumber + 1}`;
}

